<br><br>
<br>
<br>


<div class="container">
	<div class="card">
		<div class="card-header text-center">
			<%= @user.first_name %> <%= @user.last_name %>
		</div>
		<div class="card-body">
			<ul>
			<li>Description: <%= @user.description %></li>
			<li>Email: <%= @user.email %></li>
			<li>Encrypted_password: <%= @user.encrypted_password %></li>
			</ul>

			<% if @user.is_admin == true %>
			<% else %>
			<%= link_to "Modification", edit_admin_checkuser_path(@user.id), :class => "btn btn-outline-success btn-mg p-1" %>
     		<%= link_to "Delete", admin_checkuser_path(@user.id), :class => "btn btn-outline-danger btn-mg p-1", :data => { :confirm => "Are you sure?" }, :method => "delete" %>
     		<% end %>
		</div>
	</div>
</div>