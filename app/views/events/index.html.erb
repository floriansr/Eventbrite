    <%if !current_user %>
     <div style="background:transparent !important" class="jumbotron">
        <center><h1 class="display-4">Hello, world!</h1>
        <p class="lead">This is the website that we can create a simple event. If you want to create an event  for other people here and from about! Please subscribe.</p>
        <hr class="my-3">
        <p>I am sure that you will get more advantages of it</p></center>
    </div>

    <%else %>


    <div style="background:transparent !important" class="jumbotron">
      <center><h1 class="display-4 m-1 text-primary">Hello, <%=current_user.first_name%>!</h1>
      <p>Welcome on my website, let's see which events fit to your dreams! </p>
      <hr class="my-3">
      <p>You can join any events on this page. Look into it!</p><br>
      <div class=" mb-4"><%= link_to "Create event", new_event_path, :class => "btn btn-outline-danger btn-lg btn-block" %></div></center>
  </div>
    </div>


<!-- affichage des events -->

  <div class="card-columns border-warning container " >
      <% @events.each do |event| %>
        <div class="card">
            <div class="card-header text-center">
                <h5 class="card-title text-danger"><%= event.title %></h5>
            </div>
            <div class="card-body text-center">
            <p class="card-subtitle mb-3"><%= event.description[0..60] %>...</p>
            <a class="btn-outline-warning btn p-1" <%= link_to "Have a look on it", event_path(event.id)%></a>
            </div>
        </div>
      <% end %>
  </div>
<%end%>

<!-- btn-outline-warning btn p-1 -->